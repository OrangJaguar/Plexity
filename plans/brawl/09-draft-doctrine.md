# Plan 09 — Draft doctrine (locked from Q&A + research)

## Protocol

- Blind bans: each of 6 bans in one window; tool stays simple (voice dump after phase)
- Coin winner = Team A first pick
- Sequence: A1→B1→B2→A2→A3→B3; each seat ~22s
- Party leader (in-game, **not** app admin) = last pick on our side
- Other two: lower ranked Elo before higher
- Ready ~17s: loadout tweak + P11↔P11 swaps (both confirm) — later

## Scoring priorities (not pocket spam)

1. Draft step doctrine (A1 safe / B3 multi-counter)
2. Mode role budget + counters/synergy
3. Personal fit (tempered)
4. Soft meta prior
5. Mild pocket (+4) / soft avoid (−18) — never hard-kill list with avoid

## Map pool

Admin `/admin/brawledit` curated `rankedPoolByMode` is the ranked dropdown source.
