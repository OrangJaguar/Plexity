# Image elements

Curated SVG icons for the Elements → Graphics panel (Plan 2).
Icons are inlined in `src/lib/tools/image/image-elements-catalog.js` (MIT Lucide-style paths).
Shapes are procedural. Session images come from uploads already on the canvas.
